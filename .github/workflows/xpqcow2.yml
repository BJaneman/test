name: Cloud VM Windows 7 QCOW2 (FIXED)
on: 
  workflow_dispatch:

jobs:
  run-vm:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Install QEMU and noVNC
        run: |
          sudo apt-get update
          # Added python3-numpy to fix the 'client.conf' and performance issues
          sudo apt-get install -y qemu-system-x86 qemu-utils websockify novnc python3-numpy
          
      - name: 2. Download Image
        run: |
          # Downloading your specific Win 7 QCOW2 link
          # NOTE: If this step fails or is very fast, the Mediafire link has expired.
          curl -L -o win.qcow2 "https://download2330.mediafire.com/7fvbld7ppb5g6pzhJvsZfbivKOBvcX_EXL6t_bbFJDp6MjHoO54IesozHc2mFf3u9CadBDgvZdqwD9oJljih-xUHL5HSGpdx_qB8vfzueEeNWkiN907H88KOrdhFKJpl7G7n77L-0h1bNnj2wtEkt2h-lbDD1jYgcnXUDXEEkLGOrg/qltz6n680wwtz3r/w7.qcow2"
          
      - name: 3. Setup Tunnel
        run: |
          wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared-linux-amd64.deb
          # Tunnel points to the noVNC web port (6080)
          cloudflared tunnel --url http://localhost:6080 > tunnel.log 2>&1 &
          
      - name: 4. Boot VM and Launch Web Proxy
        run: |
          # A. Start QEMU (Using QCOW2 format and 4GB RAM)
          qemu-system-x86_64 -m 4G -smp 2 -cpu core2duo \
          -drive file=win.qcow2,format=qcow2 \
          -net nic,model=virtio -net user \
          -vga std -display vnc=:0 &
          
          # B. Start noVNC using the CORRECT binary path for Ubuntu
          /usr/bin/novnc_proxy --vnc localhost:5900 --listen 6080 &
          
          # C. Output the Link
          echo "============================================"
          echo "WAITING FOR CLOUDFLARE LINK..."
          sleep 30
          grep -o 'https://[-a-z0-9.]*trycloudflare.com' tunnel.log
          echo "============================================"
          
          # D. Keep alive for 6 hours
          sleep 21600
